# Cloudflare Tunnel token.
# Zero Trust dashboard > Networks > Tunnels > (your tunnel) > Install connector,
# then copy the token out of the shown command.
# Route the tunnel's public hostname to: http://app:3000
TUNNEL_TOKEN=
